import { HeaderContainer, HeaderTitle } from './styled'

export const Header = () => {
    return (
        <HeaderContainer>
            <HeaderTitle>
                weather app
            </HeaderTitle>
        </HeaderContainer>
    )
}